This is a script that wraps and optionally combines common update commands. Put it behind an alias like 'smart-up'

Requires nala and flatpak.

Not POSIX compliant.

If you don't use nala, you could modify the script. I might make the script smarter.
